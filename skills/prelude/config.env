# Universal Prelude skill — only the server address is configured here.
# Wiki identity (repo + branch) is resolved at runtime:
#   1. arguments:   sync.sh <repo> [branch]
#   2. env/config:  PRELUDE_DEFAULT_KEY (pinned) or PRELUDE_REPO + PRELUDE_BRANCH
#   3. git:         auto-detected from the surrounding repository
#   4. otherwise:   sync.sh exits 2 and the agent must ASK THE USER.
PRELUDE_SOURCE=http://10.15.33.50:8002
export PRELUDE_SOURCE
